name: CI Production

on:
  push:
    tags:
    - '*'

permissions:
  contents: read

env:
  API_IMAGE: email/api
  APP_IMAGE: email/app
  DAEMON_IMAGE: mailserver/daemon
  TAG: latest
  VERSION_TAG: ${{ github.ref_name }}
  REGISTRY: ${{ secrets.REGISTRY_NAME }}

jobs:
  deploy_production:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout PR branch
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          ref: ${{ github.ref }}

      - name: Pull changes from branch
        env:
          GIT_REF: ${{ github.ref }}
        run: git pull origin "$GIT_REF"

      - name: create app/.env file
        run: |
          echo "VITE_API_URL=${{ secrets.PROD_VITE_API_URL }}" >> app/.env
          echo "VITE_DOMAINS=${{ secrets.PROD_VITE_DOMAINS }}" >> app/.env
          echo "VITE_APP_NAME=${{ secrets.PROD_VITE_APP_NAME }}" >> app/.env
          echo "VITE_RESYNC_URL=${{ secrets.PROD_VITE_RESYNC_URL }}" >> app/.env
          echo "VITE_PRICING_URL=${{ secrets.PROD_VITE_PRICING_URL }}" >> app/.env

      - name: Build api image
        run: docker build -t $REGISTRY/$API_IMAGE:$TAG -t $REGISTRY/$API_IMAGE:$VERSION_TAG api/.

      - name: Build app image
        run: docker build -t $REGISTRY/$APP_IMAGE:$TAG -t $REGISTRY/$APP_IMAGE:$VERSION_TAG app/.
      
      - name: Build daemon image
        run: docker build -t $REGISTRY/$DAEMON_IMAGE:$TAG -t $REGISTRY/$DAEMON_IMAGE:$VERSION_TAG mailserver/daemon/.

      - name: Log in to registry
        run: echo "${{ secrets.REGISTRY_PASSWORD }}" | docker login ${{ secrets.REGISTRY_NAME }} -u ${{ secrets.REGISTRY_USERNAME }} --password-stdin

      - name: Push api:${{ env.TAG }}
        run: docker push $REGISTRY/$API_IMAGE:$TAG

      - name: Push api:${{ env.VERSION_TAG }}
        run: docker push $REGISTRY/$API_IMAGE:$VERSION_TAG

      - name: Push app:${{ env.TAG }}
        run: docker push $REGISTRY/$APP_IMAGE:$TAG

      - name: Push app:${{ env.VERSION_TAG }}
        run: docker push $REGISTRY/$APP_IMAGE:$VERSION_TAG

      - name: Push daemon:${{ env.TAG }}
        run: docker push $REGISTRY/$DAEMON_IMAGE:$TAG

      - name: Push daemon:${{ env.VERSION_TAG }}
        run: docker push $REGISTRY/$DAEMON_IMAGE:$VERSION_TAG