name: email

services:
  api:
    build: .
    container_name: email-api
    image: email-api
    restart: always
    ports:
      - ${API_PORT}:${API_PORT}
    volumes:
      - api:/var/log
    networks:
      email:
  
  app:
    build: ../app
    container_name: email-app
    image: email-app
    restart: always
    ports:
      - ${APP_PORT}:${APP_PORT}
    networks:
      email:

volumes:
  api:

networks:
  email:
    driver: bridge
    ipam:
      config:
        - subnet: ${NETWORK_SUBNET}
