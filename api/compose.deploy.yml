name: email

services:
  api:
    build: .
    container_name: email-api
    image: email-api
    restart: unless-stopped
    ports:
      - ${API_PORT}:${API_PORT}
    volumes:
      - api:/var/log
    networks:
      net:
        ipv4_address: ${API_CONTAINER_IP}
    extra_hosts:
      - "host.docker.internal:host-gateway"

  app:
    build: ../app
    container_name: email-app
    image: email-app
    restart: unless-stopped
    ports:
      - ${APP_PORT}:${APP_PORT}
    networks:
      net:
        ipv4_address: ${APP_CONTAINER_IP}

volumes:
  api:

networks:
  net:
    driver: bridge
    ipam:
      config:
        - subnet: ${NETWORK_SUBNET}
